<% if object.errors.any? %>
	<div id="error_explanation">
    <h2><%= pluralize(object.errors.count, "error")%> prohibited this 
    	<%= object.class.to_s.underscore.humanize.downcase %> 
    	from beign saved:</h2>
    <% object.errors.full_messages.each do |msg| %>
    <ul><li><%= msg %></li></ul>
    <% end %>
  </div>
<% end %>